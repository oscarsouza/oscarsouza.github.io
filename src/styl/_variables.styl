// Wrapper site
$container = rem(1080px)

// Grid
$margin-cols = 2%
$max-cols = 12
$width-cols = (100 - ($margin-cols * ($max-cols - 1))) / $max-cols

$transition = all .25s ease

// colors
$white = #ffffff
$red = #b91f27